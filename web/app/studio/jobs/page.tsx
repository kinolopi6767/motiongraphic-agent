import { Placeholder } from "@/components/placeholder";

export default function JobsPage() {
  return (
    <Placeholder
      title="Jobs"
      next="Render pipeline activity will stream here: queued, running, and delivered MP4s."
    />
  );
}