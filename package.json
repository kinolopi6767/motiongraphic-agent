{
  "name": "agentic-video-pipeline",
  "version": "0.1.0",
  "description": "Script → storyboard → animated HTML (GSAP) → deterministic MP4",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@deepgram/sdk": "^5.7.0",
    "gsap": "^3.15.0",
    "hyperframes": "^0.7.105"
  }
}
